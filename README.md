# NovaScension Landing Page

React + Tailwind + Vite landing page for novascension.com.

## Local setup

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

Cloudflare Pages settings:

- Build command: `npm run build`
- Output directory: `dist`
